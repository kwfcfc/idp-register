.git
**/node_modules
**/.svelte-kit
**/build
internal/web/assets
.env
.env.*
*.db
*.db-shm
*.db-wal
.direnv
*.zip
# Local dev tooling (own pnpm workspace + lockfile; never part of the image).
tools/
# CI config is not part of the image.
.crow/
# Local caches / editor & agent state.
.gocache/
.agent-shell/
.claude/
.DS_Store
# sops material must never enter a build context.
.sops.yaml
secrets.enc.yaml
