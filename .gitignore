node_modules
.svelte-kit
build
/docs-build/
/docs/book/book/
.env
.env.*
!.env.example
.DS_Store

# Go
/idp-register
/server
.gocache/
*.db
*.db-shm
*.db-wal

# Embedded frontend build output (generated by `pnpm -C web build`).
# Only spa/ is generated; the committed .gitkeep at the assets root keeps the
# Go package compiling before the first frontend build.
/internal/web/assets/spa/

# direnv stuff
/.direnv/
